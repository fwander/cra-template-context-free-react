import { TerminalInput } from "../grammar-lib/lib";

const VariableList: React.FC<TerminalInput> = (props) => {
    return <div style={{color: "blueviolet"}}>{props.content}</div>
}

export default VariableList;
